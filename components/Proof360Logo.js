export default function Proof360Logo({ className = '' }) {
  return (
    <span className={`logo-text ${className}`}>
      <span className="logo-p">P</span>
      <span className="logo-360">360</span>
    </span>
  );
}
