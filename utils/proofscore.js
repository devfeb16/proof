// Placeholder functions for future ProofScore logic
// These functions currently return static values and will be implemented in later phases

export function calculateProofScorePlaceholder(input) {
  // Intentionally simplistic placeholder; real algorithm will be implemented later
  return {
    score: 0,
    factors: [],
    inputSummary: input ? Object.keys(input) : [],
  };
}

export function normalizeFundingDataPlaceholder(raw) {
  // Return raw as-is for now; will normalize fields in later iterations
  return raw;
}


